    // Pin
        // Applies pined to target and pinning to Self
        // reduces target speed to 0
        // reduces target accuracy
        // only removed when the pined successfuly uses struggle action, the pinner uses a attack/ability that unpins the pinned or the pinned hits a attack/ability on the pinner